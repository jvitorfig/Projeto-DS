# Projeto Intellecta AI - Backend (Flask)

Este é o servidor backend para o projeto Intellecta AI, responsável por toda a lógica de negócio, autenticação de usuários e comunicação com a API do Gemini.

---

## 🚀 Começando

Siga estas instruções para configurar e executar o servidor em sua máquina local para desenvolvimento e testes.

### 1. Pré-requisitos

* Python 3.10+
* Um servidor PostgreSQL instalado e em execução
* Um ambiente virtual (ex: `venv`)

---

## 🛠️ Instalação e Configuração

Siga os passos abaixo para colocar o ambiente em funcionamento.

### Passo 1: Abrir o Terminal
Abra o Prompt de Comando (CMD) ou qualquer outro terminal de sua preferência.

### Passo 2: Ativar o Ambiente Virtual### Passo 2: Ativar o Ambiente Virtual
Navegue até a pasta do projeto (onde está a pasta `venv`) e ative o ambiente virtual:
* No Windows (CMD): .\venv\Scripts\activate
    
### Passo 3:Instalar as Bibliotecas
Com o ambiente virtual ativado, instale todas as dependências necessárias com o pip:
pip install flask flask-cors google-generativeai sqlalchemy "psycopg[binary]"




